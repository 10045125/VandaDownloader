apply plugin: 'com.android.library'

android {
  compileSdkVersion 25
  buildToolsVersion '28.0.3'

  defaultConfig {
    minSdkVersion 16
    targetSdkVersion 19
    versionCode 1
    versionName "1.0"
  }
}